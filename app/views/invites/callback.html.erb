	<a>Gmail Contacts</a>
	<ul>
	<% @contacts.each do |contact| %>
	  <li>
	  	<%= contact[:name] %> <%= contact[:email] %>
	  	<%= if User.where(email: contact[:email]).last != nil %>
	  		Add Friend
	  	<% else %>
	  		Invite
	  	<% end %>
	  </li>
	<% end %>
	</ul>